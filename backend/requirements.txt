fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
aiosqlite==0.20.0
psutil==6.0.0
passlib[bcrypt]==1.7.4
cryptography==43.0.1
websockets==13.1
python-multipart==0.0.9

# Optional local-inference backend. Install separately if you want real GGUF
# inference (requires a native build toolchain):
#   pip install llama-cpp-python
# The server runs and reports an honest "engine unavailable" status without it.

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
